{
	"name": "testbed-server",
	"version": "0.1.0",
	"private": true,
	"description": "TestBed Server",
	"scripts": {
		"compile": "tsc -b ./tsconfig.json",
		"watch": "tsc --watch -b ./tsconfig.json"
	},
	"dependencies": {
		"vscode-uri": "^3.0.8"
	},
	"engines": {
		"node": "*"
	}
}
