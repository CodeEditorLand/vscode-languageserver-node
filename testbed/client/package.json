{
	"name": "testbed-client",
	"version": "0.1.0",
	"private": true,
	"description": "Testbed client",
	"publisher": "vscode",
	"scripts": {
		"compile": "tsc -b ./tsconfig.json",
		"watch": "tsc -watch -b ./tsconfig.json"
	},
	"devDependencies": {},
	"engines": {
		"vscode": "^1.40.0"
	}
}
